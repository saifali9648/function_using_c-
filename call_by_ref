#include<iostream>
using namespace std;
void swap(int &a, int &b)
{
	int c=a;
	 a=b;
	 b=c;
}
int main()
{
	int firstnum=20;
	int secondnum=10;
	cout<<"before swape"<<firstnum<<"and"<<secondnum<<endl;
	swap(firstnum,secondnum);
	cout<<"after swap"<<firstnum<<"and"<<secondnum<<endl;
	return 0;
}
